import Home from "@/pages/Home.vue";
import Contact from "@/pages/Contact.vue";

const routes = [
    {
        path: '/',
        component: Home,
    },
    {
        path: '/contact',
        component: Contact,
    }
]

export default routes;
